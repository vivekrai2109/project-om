# OMNIRA RAG

This directory is reserved for ingestion pipelines, chunking strategies, embedding adapters, retrieval logic, and vector store integrations. The current MVP exposes these capabilities through the FastAPI backend with mock implementations and TODOs for pgvector.
