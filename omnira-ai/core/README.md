# OMNIRA Core Modules

This directory contains shared runtime building blocks for model routing, orchestration, memory, agents, tools, safety, and evaluation. The current MVP keeps executable code in `apps/api` first and can extract reusable packages into `core/` as the platform matures.
