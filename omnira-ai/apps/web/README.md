# OMNIRA Studio

OMNIRA Studio is the Next.js frontend for OMNIRA AI.

## Local Development

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running on `http://localhost:8000`.
